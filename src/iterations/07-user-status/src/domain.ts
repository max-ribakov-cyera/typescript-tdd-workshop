export type User = {
  status: string;
  email: string;
  name: string;
  phoneNumber: string;
  address: string;
};

export type UserRequest = {
  email: string;
  name: string;
  phoneNumber: string;
  address: string;
};
